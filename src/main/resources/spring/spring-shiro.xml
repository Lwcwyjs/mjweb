<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:util="http://www.springframework.org/schema/util"  xsi:schemaLocation="http://www.springframework.org/schema/beans 
       http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
       http://www.springframework.org/schema/util                    
       http://www.springframework.org/schema/util/spring-util-2.0.xsd"
       default-lazy-init="true">

    <description>Shiro安全配置</description>

    <!--安全管理器-->
    <bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
        <!--设置自定义Realm-->
        <property name="realm" ref="shiroDbRealm"/>
        <!--将缓存管理器，交给安全管理器-->
        <property name="cacheManager" ref="shiroEhcacheManager"/>
        <property name="sessionManager" ref="servletContainerSessionManager"/>
    </bean>
    <bean id="servletContainerSessionManager" class="org.apache.shiro.web.session.mgt.ServletContainerSessionManager"/>
    <bean id="sessionManager" class="com.commons.shiro.MySessionManager">
	 <property name="globalSessionTimeout" value="60000"/>
        <property name="sessionIdCookie">
            <bean class="org.apache.shiro.web.servlet.SimpleCookie">
                <constructor-arg value="JSESSIONID"/>
                <!-- 允许跨域访问Cookie（关键） -->
                <property name="httpOnly" value="false"/> <!-- 设为false，避免前端无法读取 -->
                <property name="secure" value="false"/> <!-- 非HTTPS环境必须设为false -->
                <property name="domain" value="192.168.3.5"/> <!-- 和后端地址一致 -->
                <property name="path" value="/mjweb"/> <!-- 和接口前缀一致 -->
                <property name="maxAge" value="-1"/> <!-- 随浏览器会话失效 -->
            </bean>
        </property>
        <property name="sessionIdCookieEnabled" value="true"/>

	</bean>

    <!-- 項目自定义的Realm -->
    <bean id="shiroDbRealm" class="com.commons.shiro.ShiroDbRealm"/>

    <!-- Shiro Filter -->
    <bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
        <!-- 安全管理器 -->
        <property name="securityManager" ref="securityManager"/>
        <!-- 默认的登陆访问url -->
        <property name="loginUrl" value="/admin/login,/admin/loginex"/>
        <!-- 登陆成功后跳转的url -->
        <property name="successUrl" value="/admin/index"/>
        <!-- 没有权限跳转的url -->
        <property name="unauthorizedUrl" value="/admin/unauth?unauth=1"/>
        <property name="filters">   
	         <util:map>   
	             <entry key="kickout" value-ref="kickoutSessionControlFilter"/>
	             <entry key="timeout" value-ref="timeoutSessionControlFilter"/>    
	         </util:map>   
	     </property>   
        <property name="filterChainDefinitions">
            <value>
                /commons/** = anon <!-- 不需要认证 -->
                /wechat/** = anon <!-- 不需要认证 -->
                /static/** = anon
                /myinfo/** = anon
                /app/** = anon
                /vehReg/** = anon
                /api/** = anon
                /scqdapi/** = anon
                /resources/** = anon
                /admin/login = anon
                /admin/loginex = anon
                /admin/video/** = anon
                /admin/logout = anon
                /admin/user/editPwdPage = anon
                /front/**=anon
                /services=anon           <!-- webservice的链接不需要进行认证 -->
                /services/**=anon        <!-- webservice ws后面的所有链接都不进行认证 -->                 
<!--                 /admin/**=authc-->
<!--              /admin/** = authc,kickout-->
               <!--  /admin/** = timeout -->
               <!--  /swagger-ui.html=anon 
                /swagger-resources=anon 
                /v2/api-docs=anon  
                /webjars/springfox-swagger-ui/**=anon   --> 
                <!-- /** = authc 需要认证 -->
            </value>
        </property>
    </bean>

    <!-- 复用同一个 EhCacheManager，避免重复加载 shiroCache 导致启动报错 -->
    <bean id="shiroCacheManagerFactory" class="org.springframework.cache.ehcache.EhCacheManagerFactoryBean">
        <property name="configLocation" value="classpath:ehcache-shiro.xml"/>
        <property name="shared" value="true"/>
    </bean>

    <!-- 用户授权信息Cache, 采用EhCache -->
    <bean id="shiroEhcacheManager" class="org.apache.shiro.cache.ehcache.EhCacheManager">
        <property name="cacheManager" ref="shiroCacheManagerFactory"/>
    </bean>

    <!-- 在方法中 注入  securityManager ，进行代理控制 -->
    <bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean">
        <property name="staticMethod" value="org.apache.shiro.SecurityUtils.setSecurityManager"/>
        <property name="arguments" ref="securityManager"/>
    </bean>

    <!-- 保证实现了Shiro内部lifecycle函数的bean执行 -->
    <bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor"/>

    <!-- AOP式方法级权限检查  -->
    <bean class="org.springframework.aop.framework.autoproxy.DefaultAdvisorAutoProxyCreator"
          depends-on="lifecycleBeanPostProcessor"/>

    <!-- 启用shrio授权注解拦截方式 -->
    <bean class="org.apache.shiro.spring.security.interceptor.AuthorizationAttributeSourceAdvisor">
        <property name="securityManager" ref="securityManager"/>
    </bean>
    <bean id="kickoutSessionControlFilter"    
	class="com.commons.shiro.KickoutSessionControlFilter">
	    <property name="cacheManager" ref="shiroEhcacheManager"/>   
        <property name="sessionManager" ref="servletContainerSessionManager"/>
	    <property name="kickoutAfter" value="false"/>   
	    <property name="maxSession" value="1"/>   
	    <property name="kickoutUrl" value="/admin/login?kickout=1"/>  
	    <property name="timeoutUrl" value="/timeout?timeout=1"/>    
	</bean>   
	 <bean id="timeoutSessionControlFilter"    
	class="com.commons.shiro.TimeoutSessionControlFilter">
	    <property name="cacheManager" ref="shiroEhcacheManager"/>   
        <property name="sessionManager" ref="servletContainerSessionManager"/>
	    <property name="kickoutAfter" value="false"/>   
	    <property name="maxSession" value="1"/>   
	    <property name="kickoutUrl" value="/admin/login?kickout=1"/>  
	    <property name="timeoutUrl" value="/timeout?timeout=1"/>    
	</bean> 
    
</beans>
